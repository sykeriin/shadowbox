{
  "name": "shadowbox",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server/index.js",
    "build:sdk": "esbuild server/sdk-entry.js --bundle --format=esm --platform=browser --external:hls.js --outfile=public/vendor/x2.mjs"
  },
  "dependencies": {
    "@reactor-models/x2": "^0.7.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "selfsigned": "^5.5.0",
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "ngrok": "^5.0.0-beta.2"
  }
}
